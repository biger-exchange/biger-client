
configure(subprojects) {

    repositories {
        mavenLocal()
        maven {url 'http://nexus.ucex.corp/repository/maven-public/' }
        mavenLocal()
        mavenCentral()
    }
    
    apply plugin: 'java'
    apply plugin: 'java-library'
    apply plugin: 'idea'
    apply plugin: 'maven'

    [compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

    group = 'com.biger'
    version = '2.4.5-2'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    task sourcesJar(type: Jar, dependsOn: classes) {
        archiveClassifier.set('sources')
        from sourceSets.main.allSource
    }
}
